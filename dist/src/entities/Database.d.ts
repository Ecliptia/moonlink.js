import { Manager } from "../../index";
export declare class Database {
    private store;
    private disabled;
    private dir;
    private snapshotPath;
    private logPath;
    private walStream?;
    private compactionIntervalMs;
    private compactionTimer?;
    private manager;
    private walBuffer;
    private readonly walBufferMaxSize;
    private walFlushInterval?;
    private readonly walFlushIntervalMs;
    private constructor();
    static create(manager: Manager): Promise<Database>;
    private init;
    private _serializeEntry;
    private _deserializeEntry;
    private _flushWALBuffer;
    private loadSnapshot;
    private replayWAL;
    private openWALStream;
    private appendLog;
    set<T>(key: string, value: T, log?: boolean): void;
    get<T>(key: string): T | undefined;
    delete(key: string, log?: boolean): boolean;
    keys(): string[];
    clear(): void;
    private compact;
    shutdown(): Promise<void>;
}
